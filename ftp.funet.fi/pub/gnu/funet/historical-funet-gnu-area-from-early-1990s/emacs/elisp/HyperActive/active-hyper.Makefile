;;; you must do these things in your .emacs file:
;;; 
;;; (setq load-path (cons ah-install-directory load-path))
;;; (load "active-hyper.requirements.el")

(find-file "/anonymous@scam.berkeley.edu:tmp/active-hyper.install.el")
(eval-current-buffer)
